// 変数の宣言・値の代入
let bodyWeight = 68;
let bodyHeight = 1.7;

// コンソールへの出力
console.log(bodyWeight);
console.log(bodyHeight);

// 四則演算
console.log(bodyWeight / (bodyHeight * bodyHeight));

// 変数の宣言・代入
bodyWeight = 42;
bodyHeight = 1.5;

// コンソールへの出力
console.log(bodyWeight);
console.log(bodyHeight);
