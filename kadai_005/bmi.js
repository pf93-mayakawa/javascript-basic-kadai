// 変数の宣言・値の代入
let bodyWeight = 68;
let bodyHeight = 1.7;

// コンソールへの出力
console.log(bodyWeight);
console.log(bodyHeight);

// 変数の宣言・四則演算
let bmi = bodyWeight / (bodyHeight * bodyHeight);

// コンソールへの出力
console.log(bmi);